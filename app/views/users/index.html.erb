<h1>User pages </h1>
<% @user.each do |item| %>
   <div>
    <span><%= item.photo %></span>
    <span>
     <h3><%= item.name %></h3>
     <p>Number of post: <%= item.posts_counter %> </p>
    </span>
   </div> 
<% end %> 

